{
  "name": "hub-api",
  "private": true,
  "type": "commonjs",
  "main": "dist/index.js",
  "engines": { "node": ">=20 <21" },
  "scripts": {
    "clean": "rimraf dist .tsbuildinfo",
    "build": "tsc -p . && cpy \"**/function.json\" dist --parents && cpy \"host.json\" dist && cpy package.json dist",
    "start": "func start --javascript"
  },
  "dependencies": {
    "@azure/functions": "^4.4.0",
    "csv-parse": "^5.5.0"
  },
  "devDependencies": {
    "cpy-cli": "^5.0.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.5.4"
  }
}